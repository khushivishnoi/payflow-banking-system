spring.application.name=fraud-service
server.port=8084

# Redis — use both keys to cover Lettuce/Jedis
spring.data.redis.host=localhost
spring.data.redis.port=6379
spring.redis.host=localhost
spring.redis.port=6379

# Kafka
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=fraud-service-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.apache.kafka.common.serialization.StringSerializer

# Fraud rules defaults
fraud.rule.max-amount=50000
fraud.rule.velocity-limit=5
fraud.rule.velocity-window-seconds=60
fraud.rule.new-payee-threshold=10000
